from CompletePartialRailways import * # This file imports from GenerateCompleteRailway already, so it doesn't need to be imported here
from GenerateMissingRailways import *
# Importing * from a file will run the code, therefore importing in the correct sequence is all that is needed
print('All railways generated')